for backend run
    uvicorn server:app --reload

for frontend run
    npm run dev

use pgadmin to mess with db if needed otherwise
to edit db manually run (in backend folder?)
    psql -U postgres -d starboy